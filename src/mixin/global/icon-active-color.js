export const iconColor = {
    computed: {
        /**进度条活跃颜色
         * 依赖：混入theme.js
         */
        iconActiveColor() {
            let color = "white";
            return color;
        },
    }
}